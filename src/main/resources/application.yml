spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    url: jdbc:postgresql://localhost:5432/message_task_db
    password: '11111111'
  jpa:
    open-in-view: 'false'
    hibernate:
      ddl-auto: validate
server:
  port: '7070'
secret: secret